# Uncomment the next line to define a global platform for your project
# platform :ios, ’10.0’

target 'ToDoList' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
# use_frameworks!

  # Pods for ToDoList
  # We can add custum librarires like AFNetworking.It automatically manage dependency . I added MBProgresHud only for example here it isn't used in demo
  
  pod 'MBProgressHUD', '~> 1.0.0'

  target 'ToDoListTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ToDoListUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
